#:kivy 1.0.9
#:import utils kivy.utils

<RootLayout>:
  canvas:
    Color:
      rgb: utils.get_color_from_hex('#222629')
    Rectangle:
      pos: self.pos
      size: self.size

<HeadsUpDisplay>:
  root: root_widget